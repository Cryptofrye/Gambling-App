namespace GamblingAppClient
{
    class Constants
    {
        static readonly string serverIP = "XXX.XXX.XXX.XXX"; //Replace this with the IP of your server running the RESTful API
        static readonly int serverPort = 1337; //Replace this with your port. 1337 is the default
        static readonly string registerUserEndpoint = "/register"; //The URL endpoint that will be used to register users with. /register is default
    }
}
